spring:
  datasource:
    driver-class-name:   org.postgresql.Driver
    url:                 jdbc:postgresql://localhost:5432/todolist_development
    username:            ${SPRING_DATASOURCE_USERNAME}
    password: